
// -- user code here --

/* --- start generated code --- */

// Generated by  1.4.4 (Phaser v2.6.2)


class Heart extends Phaser.Sprite {
	/**
	 * Heart
	 * @param {Phaser.Game} aGame A reference to the currently running game.
	 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
	 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
	 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
	 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
	 */
	constructor(aGame, aX, aY, aKey, aFrame) {
		super(aGame, aX, aY, aKey || 'hearts', aFrame  == undefined || aFrame == null? 0 : aFrame);
		this.scale.setTo(0.11, 0.11);
		this.anchor.setTo(0.5, 0.5);
		var _anim_full = this.animations.add('full', [0], 0, false);
		var _anim_empty = this.animations.add('empty', [3], 0, false);
		var _anim_oneThird = this.animations.add('oneThird', [2], 0, false);
		var _anim_twoThirds = this.animations.add('twoThirds', [1], 0, false);
		
		// public fields
		
		this.fAnim_full = _anim_full;
		this.fAnim_empty = _anim_empty;
		this.fAnim_oneThird = _anim_oneThird;
		this.fAnim_twoThirds = _anim_twoThirds;
		
	}
	
	/* sprite-methods-begin */
	// -- user code here --
	/* sprite-methods-end */
}
/* --- end generated code --- */
// -- user code here --
export default Heart;
